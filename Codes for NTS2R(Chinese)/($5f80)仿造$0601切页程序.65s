
 .ORG $5f80
 ;这段程序利用了jsr(机器码20)的一个特性
 ;就是把要返回的地址压入栈中
 ;利用这个特性 本程序来复制jsr程序后面的程序
 ;复制到$5c30里执行 然后在切页
 ;目的是解决程序不在一个映射页冲突的情况
 LDA $43	;读取$43的值到寄存器A 零页寻址
 STA $45	;把寄存器A数值传送到$45 零页寻址
 PLA		;出栈 隐含寻址
 STA $f3	;把寄存器A数值传送到$f3 零页寻址	
 PLA		;出栈 隐含寻址
 STA $f4	;把寄存器A数值传送到$f4 零页寻址
 LDY #$01	;读取数值01到寄存器Y 零页寻址
loop:
 LDA ($f3),y	;寄存器Y的零页变址 获取了压栈之前的地址数值
 STA $5c30,y	;寄存器Y的直接变址 把获取字节送到$5c30+y处
 INY		;隐含寻址 Y自增1
 BPL loop	;BPL判断标志N是否得0也就是寄存器Y是否大于80(80是负数)
 JMP $5c31	;跳转到$5c31执行 绝对寻址
 
 ;以上也就完成了仿造$0601切页程序的功能
 
 